# nfa_to_dfa_convertor
This converts a given nfa into a dfa using subset construction method
First, the user inputs the start state, symbols, final state and transitions of NFA in the format mentioned.
Next, the user clicks on convert button.
On clicking convert button, NFA is displayed. The subset construction steps are also mentioned along with DFA diagram and transition table.
